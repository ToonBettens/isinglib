"""IO backends for serialising and deserialising ``Problem`` instances.

Each submodule owns one format. ``Problem.from_*`` / ``Problem.to_*``
are the public entry points; these modules are implementation details.
"""
